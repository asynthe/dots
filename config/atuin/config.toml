auto_sync = true
sync_frequency = "5m"
sync_address = "https://api.atuin.sh"
search_mode = "fuzzy"
invert = true
enter_accept = true

# history_filter = [
#   "^secret-cmd",
#   "^innocuous-cmd .*--secret=.+",
# ]
# cwd_filter = [
#   "^/very/secret/area",
# ]

[sync]
records = true
