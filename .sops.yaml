# SOPS config for this system's secrets (dots/secrets/secrets.yaml).
# Same age identity as infra-lach's secrets — one key, multiple repos.
# See infra-lach/docs/secrets-sops.md for the add/remove-recipient process.
creation_rules:
  - path_regex: secrets/.*\.(ya?ml|json|env)$
    age: age1hp0m7d3rp7faekqftkjfqy600w38h2mtrhele0unwupnzr0yx3gs6xvvy7
